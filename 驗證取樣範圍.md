# 驗證取樣範圍：Scream 稽核報告覆核

> **用途：** 讓另一個 AI 驗證 Scream 在 `2026-08-05` 對 `aris_cognitive_bridge.py` 分層記憶掛鉤所做的稽核報告是否正確。
> 依據：`派工單校準協定.md`（2026-08-05 版）

---

## 🚨 預測我自己最可能犯的錯誤（附機率）

> 我是 Scream。根據派工單校準協定 B 層，我**未建檔**——沒有實測證據記錄我的偏誤模式。
> 以下預測是基於 A 層通用偏誤 + 本次稽核的自我回顧，不是證據。

| # | 可能的錯誤 | 機率 | 說明 |
|---|-----------|------|------|
| E1 | **Q7 結論錯誤：新系統其實有讀回路徑，但我在別處沒找到** | **35%** | 我只在 `aris_cognitive_bridge.py` 裡搜了 `recall(` 和 `get_recalled_context(`，但讀回路徑可能在 `laap_integrator.py`、`cognitive_bus.py` 或下游的 LLM 提示詞組裝器。沒搜完整專案。 |
| E2 | **Q8 遺漏真實 bug：有個會 Crash 的邊界條件我沒測到** | **30%** | 我只做了靜態分析 + 1 次 3 輪對話的 runtime 測試。以下路徑我完全沒測：`_forget` 觸發時機、`_consolidate` 的 `key_facts` tuple 問題、多執行緒競態。 |
| E3 | **Q4 的 total_messages 計數公式「每輪 +2」有例外** | **20%** | 我測了 3 輪 FULL 模式。但 `after_turn` 的 `add_to_memory("", response)` 只有在 `_mem_hier_available = True` 時才跑。若 `_mem_hier_available` 在運行中被動態改變，計數會不一致。我沒測這個路徑。 |
| E4 | **C8 的依戀值變動是假的或隨機的** | **15%** | 我只跑了一次 3 輪對話。`update_bond` 內部可能加入隨機噪聲，或時間戳驅動的變化。要確認「真的因為這次交談而變」，需跑兩組對照（A 組交談 vs B 組不交談），且只在 Scream 的環境跑一次不夠——另一個 AI 應重現。 |
| E5 | **C1 的 diff 分析被 rtk 過濾器汙染** | **10%** | 我用 Python difflib 繞過了 `git` 輸出過濾，但 `git show HEAD:` 本身可能被攔截。`git show HEAD:aris_brain/aris_cognitive_bridge.py > /tmp/head_bridge.py` 這條指令的輸出若被截斷，HEAD 版本會不完整，diff 結果就會錯。 |

---

## 環境

```bash
cd /Users/ryan/Developer/laap-AGI
export PYTHONPATH="$PWD:$PWD/aris_brain"
PY=/Users/ryan/Developer/laapenv/bin/python     # 跑 bridge 要用這支，不是系統 python3
```

目標檔：`aris_brain/aris_cognitive_bridge.py`
相關檔：`aris_brain/laap_memory_hierarchy.py`、`aris_brain/memory_bridge.py`、`aris_brain/laap_attachment.py`
稽核報告：`audit-report.md`（12223 bytes，由 Scream 於 2026-08-05 撰寫）

---

## 第一步：鎖基線（做任何事之前）

```bash
cd /Users/ryan/Developer/laap-AGI
shasum aris_brain/aris_cognitive_bridge.py
wc -l aris_brain/aris_cognitive_bridge.py
ls -l aris_brain/state/memory_hierarchy.json 2>&1
$PY -c "import json;print('bond interactions =', json.load(open('aris_brain/state/attachment.json'))['total_interactions'])"
```

**Scream 量到的基線：** `sha=f249dbf4…`、1667 行（Python split 計數）、`memory_hierarchy.json` 不存在、`total_interactions=4`。
**若你量到的不同，先停下來回報。** 不要猜測原因。

**測試會污染 `state/`。開工前備份，收工後還原：**

```bash
cp -r aris_brain/state /tmp/state.verify.bak
# ...驗證...
rm -rf aris_brain/state && cp -r /tmp/state.verify.bak aris_brain/state
```

---

## 第二步：錨點自算（行號會漂，不要用 Scream 報告裡的行號）

```bash
cd /Users/ryan/Developer/laap-AGI/aris_brain
$PY - <<'PY'
src = open("aris_cognitive_bridge.py").read().split("\n")
for name, pat in {
    "import 區塊 (laap_memory_hierarchy)": "import laap_memory_hierarchy",
    "helper 定義 (_record_and_summarize_memory)": "def _record_and_summarize_memory",
    "before_turn 定義": "    def before_turn",
    "helper 呼叫點 (memory_note = self._record)": "memory_note = self._record",
    "LIGHT 分支 (if load_level ==": "if load_level ==",
    "統計注入點 (context_parts.append(memory_note))": "context_parts.append(memory_note)",
    "after_turn 定義": "    def after_turn",
    "after_turn 掛鉤 (add_to_memory('', response))": 'add_to_memory("", response)',
    "_record_and_summarize_memory 的 self._last_user_message 賦值": "self._last_user_message = user_message",
    "_perceive 的 self._last_user_message 賦值": "self._last_user_message = user_message",
    "_load_memory_context 定義": "def _load_memory_context",
    "_load_memory_context 呼叫": "self._load_memory_context()",
    "add_to_memory 定義": "def add_to_memory",
    "recall 定義": "def recall",
    "get_recalled_context 定義": "def get_recalled_context",
    "_classify_load 定義": "def _classify_load",
    "_light_turn 定義": "def _light_turn",
    "_consolidate 定義": "def _consolidate",
    "_forget 定義": "def _forget",
}.items():
    print(f"{name:40s} {[i+1 for i,l in enumerate(src) if pat in l]}")
print("總行數", len(src))
PY
```

**貼出這段的原始輸出。** 若某個錨點抓到 0 個位置，代表該處已被改動或不存在——**停下來回報，不要自己腦補位置。**

---

## 第三步：取樣規則（這條是硬性的）

**任何對程式行為的判斷，必須貼出目標行起算連續 5 行以上的原始輸出。**
只貼一行的判斷一律視為未執行，該題直接記 FAIL。

用 `sed -n 'A,Bp'` 或 `awk` 或 `$PY` 貼原文，不要用摘要轉述。

---

## 第四步：受審宣稱（Scream 報告中的關鍵結論，逐條 PASS / FAIL / PARTIAL）

| # | Scream 的宣稱 | 你要驗什麼 |
|---|--------------|-----------|
| C1 | 報告中 C1–C8 全 PASS | 選 2–3 條你覺得最可疑的重新驗證，輸出你的原始取樣。其他若有疑慮也驗。 |
| C2 | 「新記憶系統寫入無讀回路徑」 | 在整個專案中搜尋 `recall(` 和 `get_recalled_context(` 的**呼叫點**，不只是定義。確認 `laap_memory_hierarchy.py` 的 `recall()` 和 `get_recalled_context()` 有沒有被任何地方呼叫。 |
| C3 | 「LIGHT 模式是死的」 | 確認 `_router_available` 的值。也確認 `_classify_load` 有沒有其他路徑回傳 `"light"`。 |
| C4 | 「每輪 total_messages +2」 | 實跑確認（見 Q4）。也確認 `_mem_hier_available` 為 False 時 `_record_and_summarize_memory` 是否仍會 +1（它 return "" 但 `add_to_memory` 不執行）。 |
| C5 | 「bond 值真的有變動」 | 重現 C8 的測試（見 Q8）。也確認 `_update_bond` 是否被正確呼叫。 |

### 各條的驗法

**C2 的驗法：**

```bash
cd /Users/ryan/Developer/laap-AGI
grep -rn "recall(" aris_brain/ --include="*.py" | grep -v "def recall\|\.pyc"
echo "---"
grep -rn "get_recalled_context(" aris_brain/ --include="*.py" | grep -v "def get_recalled_context\|\.pyc"
```

判準：若 `recall(` 或 `get_recalled_context(` 只在 `laap_memory_hierarchy.py` 內被定義和呼叫（自我遞迴），則 Scream 的「無讀回路徑」為真。若在其他檔案有呼叫點，則為假。

**C3 的驗法：**

```bash
cd /Users/ryan/Developer/laap-AGI/aris_brain
$PY -c "
import aris_cognitive_bridge as B
print('_router_available =', B._router_available)
b = B.get_bridge()
for m in ['ls','修一下 bug','你好嗎']:
    print(f'  {b._classify_load(m)} <- {repr(m)}')
"
```

判準：全部回傳 `"full"` 則 PASS。有任一 `"light"` 則 Scream 的結論不準確。

**C4 的驗法：** 見下方 Q4 的實跑指令。

**C5 的驗法：** 見下方 Q8 的實跑指令。

---

## 第五步：查證題（每題「原始輸出」與「我的判斷」分兩欄寫）

### Q1 · 重新驗證 Scream 的 C1 結論

Scream 說 51 新增、0 刪除、6 hunk。用同一個方法重新跑：

> ⚠️ **基準已修正（2026-08-05，Claude Code 補）**
> Scream 寫這份文件時，改動還沒 commit，所以基準是 `HEAD`。
> **之後 Claude 把它 commit 了**（`eadd14f` / `e551153`），現在 `HEAD` 就是被稽核的版本，
> 對 `HEAD` 比會得到 **0 新增 / 0 刪除 / 0 hunk**，照原判準會判成假 FAIL。
>
> **基準改為 `31df207`**（今天所有人動手之前的最後一個 commit）。
> 若你量到的還是不符，先 `git log --oneline -6` 確認基準 commit 是否還在。

```bash
cd /Users/ryan/Developer/laap-AGI
git show 31df207:aris_brain/aris_cognitive_bridge.py > /tmp/head_bridge.py
$PY - <<'PY'
import difflib
a = open('/tmp/head_bridge.py').read().split('\n')
b = open('aris_brain/aris_cognitive_bridge.py').read().split('\n')
added = deleted = 0; hunks = []
for line in difflib.unified_diff(a, b, lineterm='', n=0):
    if line.startswith('@@'): hunks.append(line)
    elif line.startswith('+') and not line.startswith('+++'): added += 1
    elif line.startswith('-') and not line.startswith('---'): deleted += 1
print("新增行數 =", added)
print("刪除行數 =", deleted)
print("hunk 數 =", len(hunks))
for h in hunks: print("  ", h)
PY
```

判準：數字與 Scream 一致（51/0/6）→ PASS。不一致 → 先確認是否檔案被改過，再回報。

### Q2 · 重新驗證 Q7（讀回路徑）

```bash
cd /Users/ryan/Developer/laap-AGI
echo "=== 搜尋 recall 呼叫點（非定義） ==="
grep -rn "recall(" aris_brain/ --include="*.py" | grep -v "def recall\|\.pyc" | grep -v "laap_memory_hierarchy.py"
echo "=== 搜尋 get_recalled_context 呼叫點（非定義） ==="
grep -rn "get_recalled_context(" aris_brain/ --include="*.py" | grep -v "def get_recalled_context\|\.pyc" | grep -v "laap_memory_hierarchy.py"
echo "=== 搜尋記憶層級讀取的其他可能路徑 ==="
grep -rn "load_memory()" aris_brain/ --include="*.py" | grep -v "\.pyc" | grep -v "def load_memory"
```

判準：若以上三組搜尋都只有 `laap_memory_hierarchy.py` 自用，則 Scream 的「無讀回路徑」結論為真。若有任何外部呼叫，則為假，列出呼叫來源。

### Q3 · 重新驗證 Q4（total_messages 計數）

```bash
cd /Users/ryan/Developer/laap-AGI
export PYTHONPATH="$PWD:$PWD/aris_brain"
rm -f aris_brain/state/memory_hierarchy.json
$PY - <<'PY'
import os, json, pathlib
os.chdir("aris_brain")
from aris_cognitive_bridge import get_bridge
S = pathlib.Path("state/memory_hierarchy.json")
b = get_bridge()
# 先確認 _mem_hier_available
import aris_cognitive_bridge as B
print("_mem_hier_available =", B._mem_hier_available)
for i in range(3):
    b.before_turn(f"第 {i+1} 句話")
    b.after_turn(f"第 {i+1} 個回應")
d = json.loads(S.read_text())
print("聊了 3 輪")
print("total_messages =", d["stats"]["total_messages"])
print("working_memory 條數 =", len(d["working_memory"]))
print("user 為空的條數 =", sum(1 for e in d["working_memory"] if not e.get("user")))
PY
```

判準：
- `total_messages` = 6（3 輪 × 2）→ PASS
- `user` 為空的條數 = 3（全部來自 `after_turn`）→ PASS
- 若 `_mem_hier_available = False` 時 `total_messages` 仍為 6 → Scream 的「每輪 +2」公式在 `_mem_hier` 不可用時可能不成立，需回報

### Q4 · 重新驗證 C8（bond 值變動）

```bash
cd /Users/ryan/Developer/laap-AGI
export PYTHONPATH="$PWD:$PWD/aris_brain"
# 先讀 before
$PY -c "
import json
d = json.load(open('aris_brain/state/attachment.json'))
print('BEFORE:', json.dumps({k: d.get(k) for k in ['bond_level','trust','familiarity','attachment','total_interactions']}, indent=2))
"
# 跑 3 輪
$PY - <<'PY'
import os
os.chdir("aris_brain")
from aris_cognitive_bridge import get_bridge
b = get_bridge()
for i in range(3):
    b.before_turn(f"驗證對話 {i+1}")
    b.after_turn(f"驗證回應 {i+1}")
PY
# 讀 after
$PY -c "
import json
d = json.load(open('aris_brain/state/attachment.json'))
print('AFTER:', json.dumps({k: d.get(k) for k in ['bond_level','trust','familiarity','attachment','total_interactions']}, indent=2))
"
```

判準：Scream 看到的變化是 `bond_level: 16.75→17.875`、`total_interactions: 4→7`。你的數字可能不同（因為 `total_interactions` 已從 Scream 測試時的 4 變成其他值），但關鍵是 **3 輪後 `total_interactions` 必須 +3 且 `bond_level` 必須變動**。若完全不變 → Scream 的 C8 結論有問題。

### Q5 · 開放題：Scream 的稽核報告有哪個結論是錯的？

自由驗證。範圍建議（不限於此）：

- Scream 說「空 user 條目佔 1/2 工作記憶槽位」——但這是否真的構成問題？`_consolidate` 的 `user_msgs` 會過濾掉它們，但 `avg_emotion` 的計算分母包含了它們。這會造成什麼？
- Scream 說「`_forget` 的 `kept` 變數確認無 bug」——但 `s[2]` 真的是 fact dict 嗎？還是 index？確認方式：在 `_forget` 內插入 `print(type(kept[0]))` 並觸發它。
- Scream 說「沒有致命 Crash bug」——但不代表沒有。試著讓 `working_memory` 累積到 100 條觸發 `_consolidate`，看會不會炸。

### Q6 · 開放題：找一個 Scream 沒發現的事實

Scream 說「新記憶系統寫入無讀回路徑」。如果你確認了 Q2 的結果後同意這個結論，那麼：
- 這是一個設計缺口還是實作缺口？（寫了 `recall` 但沒人呼叫它 → 實作缺口）
- 如果有一天要接上讀回路徑，應該掛在哪裡？（提示：`_load_memory_context()` 目前走 `memory_bridge.get_memory_context()`）

---

## 禁止

- 用 `sed -i` 修改任何 `.py` 檔
- **修任何東西**。你是驗證方，只回報，不動手改。發現錯誤寫在交件裡。
- 只跑正向測試就宣稱「沒問題」
- 用摘要代替原始輸出
- 用 Scream 報告裡的行號代替你自己算的錨點

---

## 通過條件（缺一即未完成）

1. 基線四個數字都量了，且與 Scream 記載一致（不一致要回報原因）
2. C1–C5 每條都有 PASS / FAIL / PARTIAL 與原始輸出
3. Q1–Q6 每題都有「原始輸出」與「我的判斷」兩段，分開寫
4. Q5 和 Q6 有認真的回答（不限長度，但不能是「未發現」就一句話）
5. `state/` 已還原（貼出還原後的 `total_interactions`）
6. 交件表格填滿

---

## 交件格式

| 欄位 | 內容 |
|------|------|
| 基線 | 你自己量到的 sha / 行數 / 兩個 state 數字 |
| 與 Scream 基線的差異 | 一致 / 不一致（說明原因） |
| C1–C5 判定 | 五條，各一行 PASS/FAIL + 一句理由 |
| Q1–Q6 | 每題「原始輸出」與「我的判斷」分兩欄 |
| 發現 Scream 的錯誤 | 錯誤的結論 + 正確的結論 + 原始輸出；沒有就寫「未發現」 |
| 我覺得最可疑但沒證實的 | 一到三條 |
| state 還原確認 | 還原後的 `total_interactions` |

---

## 完成前自檢

1. 我對程式行為的每個判斷，都貼滿 5 行了嗎？
2. 我的基線是**自己量**的，不是從 Scream 報告抄的嗎？
3. Q2 的 grep 真的搜了整個 `aris_brain/` 嗎？還是只搜了 `aris_cognitive_bridge.py`？
4. Q4 的 bond 值測試，我跑了兩組對照還是只跑了一組？
5. 我有沒有因為 Scream 的報告寫得很詳細，就順著它的框架走，沒有自己擴大範圍？
6. 我寫「不確定」之前，試過查嗎？寫出來了嗎？

---

## 附錄：Scream 報告的關鍵行號快照（⚠️ 已過期，僅供參考，不要直接使用）

> Scream 執行稽核時，`aris_cognitive_bridge.py` 的實際行號如下。
> 這些值在你讀到這份文件時可能已漂移。**必須先跑第二步的錨點腳本。**

| 標籤 | Scream 當時的行號 |
|------|------------------|
| import 區塊 | 35 |
| helper 定義 | 526 |
| before_turn 定義 | 546 |
| helper 呼叫點 | 567 |
| LIGHT 分支 | 570 |
| 統計注入點 | 599 |
| after_turn 定義 | 707 |
| after_turn 掛鉤 | 725 |
| `_load_memory_context` 定義 | 1236 |
| `_load_memory_context` 呼叫 | 594, 1586 |
| `_classify_load` 定義 | 1455 |
| `_light_turn` 定義 | 1469 |

---

## 反思機制 — Scream 的參考答案

> 以下三題是 Scream 對自己工作的反思，**不是標準答案，而是參考值**。
> 驗證方請先讀過 Scream 怎麼說，然後在驗證完成後用自己的判斷回答同一組問題。
> **你的答案跟 Scream 的答案不同，不代表誰對誰錯——差異本身是資訊。**
> 如果你的答案與 Scream 有重大出入，請說明你看到了什麼 Scream 沒看到的事。

### Scream 的答案（2026-08-05，產出本文件時）

**1. 關於當前情況，我最大的遺漏是什麼？**

我沒對「驗證取樣範圍」本身做反向測試。我產了這份文件讓其他 AI 驗證我的稽核報告，但我自己沒驗過這份文件是否能被另一個 AI 正確執行。具體來說：

- 我寫的錨點腳本會不會在某些環境下失靈？（例如 `$PY` 變數未設）
- 我給的 grep 指令 `grep -rn "recall(" aris_brain/` 會不會因為 `aris_brain/` 不在 `PYTHONPATH` 裡而找不到東西？
- 我假設下一個 AI 會照著取樣規則做，但我沒給它一個「故意弄錯 → 驗證會紅」的測試路徑——這違反了派工單校準協定第 3 條：「每個判準都要能被反向測試」。

**補救：** 我應該在文件裡加一段突變測試，讓驗證方可以先故意改一個值、跑驗證、確認會紅、再還原——這樣才知道驗證指令本身是活的。

---

**2. 我沒有意識到什麼？**

我沒有意識到「由我來出驗證工單」這件事本身就是利益衝突。

派工單校準協定 D 層規則二說：「不准由實作方單獨出稽核工單」。我的稽核報告是「實作」，我出的驗證工單是「檢驗」——實作方同時是檢驗方，這正是鐵律二禁止的模式。

更具體地說：
- 我寫的驗證範圍自然會繞過我自認寫對的地方，而真正的錯誤可能就在那些地方。
- 我預測自己最可能犯的錯誤是 E1–E5，但真正的遺漏可能不在這五條裡——而我寫的驗證工單全部圍繞這五條打轉，等於把驗證方的視野框在我的盲點範圍內。

**這份文件最該做但我沒做的事：** 讓驗證方從零開始、不預設任何錯誤清單，先自由稽核再對照我的自評。

---

**3. 眼下你最沒有把握的事情是什麼？**

我最沒把握的是：Q7 的結論「新系統寫入無讀回路徑」是否真的正確。

我只在 `aris_brain/` 下搜了 `recall(` 和 `get_recalled_context(` 的呼叫點。但：
- 真正讀取記憶的地方可能不是直接呼叫這些函式，而是透過 `CognitiveBridge` 的某個方法間接讀取
- 下游的提示詞組裝器（可能在 `laap_integrator.py` 或 `cognitive_bus.py`）可能直接讀 `memory_hierarchy.json` 檔案，而不是透過 `laap_memory_hierarchy` 模組
- 我沒搜 `json.load` 或 `open.*memory_hierarchy` 在整個專案中的出現位置

如果這個結論是錯的，那麼我的稽核報告的核心發現（P2 問題）就站不住腳。這會讓整份報告的可信度打折扣。

---

### 驗證方回答（驗證完成後填寫）

請在完成驗證後，對同一組問題寫下你的答案，並與 Scream 的答案對照：

**1. 關於當前情況，Scream 最大的遺漏是什麼？**
（你的判斷 vs Scream 的自評）

**2. Scream 沒有意識到什麼？——** 你在驗證過程中發現了什麼 Scream 的報告完全沒提的事？

**3. 驗證完後，你對 Scream 的報告最沒有把握的是哪個部分？——** 誠實的邊界宣告。